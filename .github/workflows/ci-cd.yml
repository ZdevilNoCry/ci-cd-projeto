name: Teste CI/CD

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      
name: Checkout
      uses: actions/checkout@v3

      
name: Exibir mensagem
      run: echo "🚀 Workflow funcionando no seu projeto!"
